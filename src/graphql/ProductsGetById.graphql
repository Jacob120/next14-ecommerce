query ProductGetById($id: ID!) {
	product(id: $id) {
		id
		name
		description
		categories {
			name
			id
		}
		images {
			url
			alt
		}
		price
		rating
		reviews {
			author
			description
			email
			id
			rating
			createdAt
			updatedAt
		}
	}
}
